# ConvoQuest 润色报告

> 按 **UI/UX → Prompt → 边缘情况 → 代码质量 → 游戏机制** 顺序整理。
> 每项建议带有具体的实施提示词，可直接交给对应 agent 执行。

---

## 方向 1: UI/UX 优化

### 1a. 答案徽章弹出动画
- [ ] 为 `.answer-badge` 添加 `scale` 弹出动画

**文件**: `public/css/style.css`

**提示词**:
```
在 style.css 的 .answer-badge 规则后添加弹出动画：
- 新增 @keyframes badgePop，从 scale(0.3) opacity(0) 到 scale(1.05) 再到 scale(1)
- 给 .answer-badge 添加 animation: badgePop 0.35s cubic-bezier(0.34, 1.56, 0.64, 1)
- 猜对时用绿色脉冲，猜错时用红色抖动（可复用现有 shake），接近时用金色闪烁
```

---

### 1b. 结果页面评分揭示动画
- [ ] 分数数字从 0 递增滚动到最终分数
- [ ] 称号卡片翻转揭晓效果

**文件**: `public/css/style.css`、`public/js/ui.js`

**提示词**:
```
CSS 部分：
- 新增 .score-reveal 类，数字使用等宽字体
- 新增 @keyframes countUp（无需实际 keyframe，用 JS 驱动）
- 新增 .title-card-flip，使用 perspective + rotateY 实现翻转效果
  - 正面显示 "?"，背面显示称号文字
  - transition: transform 0.6s

JS 部分（ui.js）：
- 新增函数 animateScore(element, from, to, duration)
  - 使用 requestAnimationFrame 在 duration 毫秒内将数字从 from 递增到 to
  - 默认 duration=1200
- 新增函数 flipReveal(element, content)
  - 先翻转到背面（rotateY 90deg），替换内容，再翻回正面

调用方式（在各模式的 showResult 中）：
- 将分数元素传给 animateScore，从 0 滚动到实际分数
- 将称号元素传给 flipReveal，延迟 1.5 秒后翻转揭晓
```

---

### 1c. AI 思考时的脉动指示器
- [ ] 替换 spinner + 静态文字为脉动打字指示器

**文件**: `public/css/style.css`

**提示词**:
```
修改 .loading 的样式：
- 保持 spinner 不变
- 将 span 文字改为三个跳动的圆点
- 使用 CSS 实现：.loading span::after 内容为 "..."
- 新增 @keyframes pulse，让三个点依次上下跳动（用 animation-delay 错开）
- 或者更简单：让 "AI 思考中" 文字有 opacity 呼吸灯效果（0.4↔1循环）

同时在 HTML 的所有 loading 元素中，将 <span>AI 思考中...</span> 统一改为：
<span class="thinking-text">AI 思考中<span class="dot-pulse"></span></span>

CSS 中：
.dot-pulse::after {
  content: '';
  animation: dotPulse 1.4s infinite;
  /* 用 steps() 依次显示 . .. ... */
}
```

---

### 1d. 屏幕切换淡入淡出过渡
- [ ] 6 个 screen 切换时添加 fade 过渡

**文件**: `public/css/style.css`、`public/js/ui.js`

**提示词**:
```
CSS 部分：
- .screen 默认 opacity: 0; transform: translateY(12px); transition: opacity 0.3s, transform 0.3s; pointer-events: none; position: absolute;（覆盖全屏）
- .screen.active 改为 opacity: 1; transform: translateY(0); pointer-events: auto;
- 注意：当前 .screen 用 display:none 切换，需要改为 position:fixed + opacity 方案
  或者更安全的做法：给 body 加一个 .transitioning 类，在 showScreen 中先 fade out 再 fade in

JS 部分（ui.js 的 showScreen 函数）：
- 修改 showScreen(id) 函数：
  1. 当前 active screen 添加 .fade-out 类（opacity→0）
  2. 300ms 后移除 .fade-out，隐藏旧 screen
  3. 显示新 screen 并添加 .fade-in 类（opacity→1）
  4. 300ms 后移除 .fade-in
- 使用 CSS transition 而非 JS 动画

备选方案（更简单）：
- 只给新出现的 screen 加一个 fadeIn 动画（已有 @keyframes fadeIn）
- 在 showScreen 中，先设置 display，然后添加 .entering 类触发动画
- 这个方案改动最小，只改 CSS + showScreen 函数
```

---

### 1e. 移动端侧边栏可折叠
- [ ] 添加折叠/展开按钮
- [ ] 折叠时显示一行统计摘要

**文件**: `public/index.html`、`public/css/style.css`、`public/js/ui.js`

**提示词**:
```
HTML 部分（以 host-sidebar 为例，其他 sidebar 同理）：
- 在 .host-sidebar 顶部添加一个折叠按钮：
  <button class="sidebar-toggle" onclick="toggleSidebar('host')">
    <span class="toggle-summary" id="host-summary">❓3 | 📜2/8 | 🎲1</span>
    <span class="toggle-arrow">▼</span>
  </button>
- 在 sidebar 内容外层包一个 .sidebar-body

CSS 部分：
- .sidebar-toggle：全宽按钮，显示统计摘要，点击切换
- .sidebar-body：默认展开，折叠时 height: 0; overflow: hidden; transition: height 0.3s
- @media (max-width: 768px) 下默认折叠
- .sidebar.collapsed .sidebar-body { height: 0; }
- .sidebar.collapsed .toggle-arrow { transform: rotate(180deg); }

JS 部分（ui.js）：
- 新增函数 toggleSidebar(mode)
  - 切换 sidebar 的 .collapsed 类
  - 更新 toggle-summary 的文字为当前统计数据
- 新增函数 updateSidebarSummary(mode)
  - 从 GameState 读取当前统计数据，格式化为紧凑摘要
  - 在各 updateXxxStats 函数中调用
```

---

### 1f. 聊天消息方向性入场动画
- [ ] 用户消息从右滑入、AI 消息从左滑入、系统消息淡入

**文件**: `public/css/style.css`

**提示词**:
```
替换现有的单一 fadeIn 动画为方向性动画：

@keyframes slideInRight {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes slideInLeft {
  from { opacity: 0; transform: translateX(-20px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(6px); }
  to { opacity: 1; transform: translateY(0); }
}

应用到消息类：
- .msg-ai { animation: slideInLeft 0.3s ease; }
- .msg-user { animation: slideInRight 0.3s ease; }
- .msg-system { animation: fadeIn 0.3s ease; }
- .msg-close, .msg-wrong, .msg-blind { animation: slideInLeft 0.3s ease; }
- .msg-guess { animation: fadeIn 0.4s ease; }
- .answer-badge { animation: badgePop 0.35s cubic-bezier(0.34, 1.56, 0.64, 1); }

注意：确保保持现有的 white-space: pre-wrap 和其他消息样式不变。
```

---

## 方向 2: Prompt 优化

### 2a. AI 出题 — 人物画像提取增强
- [ ] 优化 `aiHostAnswer` prompt 中的 portrait 提取规则

**文件**: `public/js/prompts.js` — `PROMPTS.aiHostAnswer`

**提示词**:
```
当前 portrait 规则是"用简洁的肯定句描述（如'非欧洲人'而非'不是欧洲'）"。
这导致否定回答时画像信息不够积极。

修改 portrait 规则为：

portrait规则（仅当answer不是"请重新提问"时填写）：
- 从本次问答中提取关于秘密人物的确定信息
- 类别使用固定名称：时代、地域、性别、领域、身份、事迹、生平、其他
- 每个类别只保留一条最精确的信息，不推断
- 如果本次问答没有新增确定信息，portrait为空 {}
- 重要：用积极肯定句描述已知事实，而非否定句
  - 用户问"此人是女性吗？"答"否" → {"性别":"男性"}（不要写"非女性"）
  - 用户问"此人来自欧洲吗？"答"否" → {"地域":"非欧洲"}（地域可以用否定，因为没有更具体的肯定信息）
  - 用户问"此人是皇帝吗？"答"是" → {"身份":"皇帝"}
  - 用户问"此人活跃于公元前吗？"答"是" → {"时代":"公元前"}
- 同一类别的旧信息如果被新信息取代，只保留新信息
  - 已有 {"地域":"非欧洲"}，现在确认了"来自中国" → 更新为 {"地域":"中国"}

同时更新示例回复部分，增加否定回答的正确示例。
```

---

### 2b. AI 猜测 — 二分法加强 + 序号一致性
- [ ] 在 `aiGuessSystem` 中增加思维链强制规则
- [ ] 在 `aiGuessTurn` 中强制使用传入的序号

**文件**: `public/js/prompts.js` — `PROMPTS.aiGuessSystem`、`PROMPTS.aiGuessTurn`

**提示词**:

**aiGuessSystem 修改** — 在"二分法实操方法"之后添加：
```
## 思维链强制要求（最高优先级）
在输出问题之前，你必须在 JSON 的思考过程中完成以下自检：
1. 写出"如果用户回答'是'，排除了什么？"和"如果用户回答'否'，排除了什么？"
2. 如果任何一端排除了不到当前候选池的20%，换一个问题
3. 确认该问题不与已问过的问题重复或重叠

这个自检不需要在给用户的文字中显示，但必须在你的推理中完成。
```

**aiGuessTurn 修改** — 在回复格式部分强化：
```
回复格式：
第一行：JSON推理状态（必须包含candidates字段）
第二行起：给用户看的提问/猜测（中文），问题以「${questionsAsked + 1}. 」开头
注意：序号必须是 ${questionsAsked + 1}，不要自己编号！
```

---

### 2c. 海龟汤出题 — 防止经典套路重复
- [ ] 在 `hostGenerate` prompt 中加入经典套路黑名单

**文件**: `public/js/turtle-prompts.js` — `TURTLE_PROMPTS.hostGenerate`

**提示词**:
```
在"规则"部分增加一条：

- 不得使用以下经典海龟汤套路或其直接变体：
  - 男人喝海龟汤后自杀（原版海龟汤）
  - 盲人坐飞机 / 电梯停在某层
  - 一个人在沙漠里死了旁边有火柴/背包
  - 企鹅肉 / 沙漠里的尸体拿着半根火柴
  - 跳楼的人 / 看到什么后跳楼
  - 电话亭 / 水草
- 你的题目必须有至少一个完全出人意料的转折
- 鼓励使用以下题材创造新颖谜题：职业特殊性、文化差异、时间错位、感官缺失、身份误解、非常规的人际关系
```

---

### 2d. 复盘阶段 — 个性化对话
- [ ] 改造 `aiHostReview`、`aiGuessReview`、`hostReview`、`guessReview` 四个 prompt

**文件**: `public/js/prompts.js`、`public/js/turtle-prompts.js`

**提示词**:

**以 aiHostReview 为例（猜赢了），改为：**
```
游戏结束！玩家成功猜出了你选择的历史人物。

先做一个简短的数据评价（2-3句话）：
- 秘密人物：${figure.name_cn} / ${figure.name_en}
- 玩家用${guessesUsed}次猜测、${host.questionsAsked}次提问、${hintsRevealed}条线索
- 最终得分：${finalScore}分，称号：${title}

评价参考：
- 猜测次数 < 最大次数的30% 且 线索 < 3条 → "你几乎不需要帮助就猜到了，太厉害了"
- 猜测次数 > 70% 或 线索 > 6条 → "过程有点曲折，但你坚持下来了"
- 盲猜命中 → 特别表扬盲猜的勇气

然后自然地聊2-3个话题（不要列清单，像朋友聊天一样）：
- 这个历史人物有什么有趣的冷知识或鲜为人知的故事
- 玩家的提问策略是否有效，有没有更好的提问方式
- 与这个人物相关的其他历史人物或事件

保持轻松有趣的风格，不要用"请和玩家讨论"这种指令语气。
```

**其他三个 review prompt 同理改造**，核心变化：
- 去掉"请和玩家讨论：1. 2. 3."这种列表式指令
- 改为先给数据评价，再自然展开话题
- 语气从"主持人总结"改为"朋友聊天"

---

### 2e. 海龟汤 AI 猜测 — 防止过早锁定
- [ ] 在 `guessSystem` 中增加开放性思维规则

**文件**: `public/js/turtle-prompts.js` — `TURTLE_PROMPTS.guessSystem`

**提示词**:
```
在"提问策略"部分，第1-5问"确认基本框架"之后增加一条硬规则：

### 强制开放性规则
- 在你确认至少以下5个基本事实之前，禁止发起正式猜测：
  1. 涉及的人物数量
  2. 是否涉及生死
  3. 时间/时代背景
  4. 地点/场景
  5. 核心事件类型
- 即使你觉得自己已经猜到了答案，也必须先通过提问验证至少5个事实
- 过早猜测是海龟汤最大的陷阱——很多汤面有"看似如此实则不然"的设计

同时在"第19-20问"部分修改：
- 将"如果信心>80%，发起正式猜测"改为"如果信心>90%且已确认至少8个事实，发起正式猜测"
```

---

## 方向 4: 边缘情况处理

### 4a. 网络请求重试机制
- [ ] 为 `chatStream` 和 `chatFull` 添加自动重试

**文件**: `public/js/api.js`

**提示词**:
```
修改 api.js，添加重试逻辑：

1. 新增全局配置：
   const API_CONFIG = {
     maxRetries: 1,
     retryDelay: 1000,
     timeout: 30000
   };

2. 新增辅助函数：
   function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

   function isRetryableError(err) {
     const msg = (err.message || '').toLowerCase();
     return msg.includes('network') || msg.includes('fetch') ||
            msg.includes('timeout') || msg.includes('503') || msg.includes('429');
   }

   function friendlyErrorMessage(err) {
     const msg = (err.message || '').toLowerCase();
     if (msg.includes('network') || msg.includes('fetch')) return '网络连接失败，请检查网络';
     if (msg.includes('timeout')) return '请求超时，请稍后再试';
     if (msg.includes('401') || msg.includes('key')) return 'API Key 无效或已过期';
     if (msg.includes('429')) return '请求太频繁，请稍后再试';
     if (msg.includes('500')) return '服务器繁忙，请稍后再试';
     return '请求失败，请重试';
   }

3. 修改 chatFull：用 for 循环包裹，失败时如果 isRetryableError 且重试次数未满，await sleep 后重试
4. 修改 chatStream：同上逻辑，但注意 stream 的错误处理在 try-catch 中
5. 保持原有的错误抛出接口不变，只是错误消息改为 friendlyErrorMessage(err)
```

---

### 4b. AI 输出格式容错
- [ ] 增强 JSON 解析的鲁棒性

**文件**: `public/js/ui.js`

**提示词**:
```
在 ui.js 中新增一个通用的 JSON 提取函数，替换各处的 raw.match + JSON.parse：

function extractJSON(text) {
  // 1. 尝试直接解析（可能整个文本就是 JSON）
  try { return JSON.parse(text.trim()); } catch {}

  // 2. 提取第一个完整的 {...} 块
  const bracePattern = /\{[^{}]*((?:\{[^{}]*\}[^{}]*)*)\}/g;
  let match;
  while ((match = bracePattern.exec(text)) !== null) {
    try { return JSON.parse(match[0]); } catch {}
  }

  // 3. 尝试修复常见问题：尾逗号、单引号、缺少引号
  const simpleMatch = text.match(/\{[\s\S]*?\}/);
  if (simpleMatch) {
    let fixed = simpleMatch[0]
      .replace(/,\s*([}\]])/g, '$1')  // 移除尾逗号
      .replace(/'/g, '"');              // 单引号→双引号
    try { return JSON.parse(fixed); } catch {}
  }

  return null;
}

然后在以下位置替换使用：
- ai-host-mode.js start() 中的 figure 解析
- ai-host-mode.js handleInput() 中的 answer 解析
- ai-guess-mode.js parseResponse() 中的 JSON 解析
- turtle-soup.js 中的所有 puzzle/answer JSON 解析

替换模式：
  // 旧
  const jsonMatch = raw.match(/\{[\s\S]*\}/);
  if (!jsonMatch) throw new Error('无法解析');
  figure = JSON.parse(jsonMatch[0]);
  // 新
  const parsed = extractJSON(raw);
  if (!parsed) throw new Error('无法解析');
  figure = parsed;
```

---

### 4c. 重复提问检测
- [ ] 在 AI 出题和海龟汤出题模式中检测重复问题

**文件**: `public/js/ai-host-mode.js`、`public/js/turtle-soup.js`

**提示词**:
```
在 ai-host-mode.js 的 handleInput 函数中，在发送给 AI 之前增加检测：

// 在 const isGuess = this.looksLikeGuess(trimmed); 之前插入：
const normalizedInput = trimmed.replace(/[？?！!。，,、]/g, '').toLowerCase();
const isDuplicate = GameState.host.qaHistory.some(qa => {
  const normalizedQ = qa.question.replace(/[？?！!。，,、]/g, '').toLowerCase();
  return normalizedQ === normalizedInput ||
         (normalizedQ.length > 5 && normalizedInput.length > 5 &&
          (normalizedQ.includes(normalizedInput) || normalizedInput.includes(normalizedQ)));
});
if (isDuplicate && !this.looksLikeGuess(trimmed)) {
  addMsg($('#host-chat-area'), '你之前好像问过类似的问题了，试试换个角度提问？', 'system');
  return;
}

在 turtle-soup.js 的 handleInput 函数中做类似处理：
const normalizedInput = trimmed.replace(/[？?！!。，,、]/g, '').toLowerCase();
const isDuplicate = GameState.turtle.knownInfo.some(info => {
  const normalizedQ = info.question.replace(/[？?！!。，,、]/g, '').toLowerCase();
  return normalizedQ === normalizedInput;
});
if (isDuplicate) {
  addMsg($('#turtle-host-chat'), '这个问题你已经问过了，试试换个角度。', 'system');
  return;
}
```

---

### 4d. 输入空内容防护
- [ ] 统一所有输入框的空值检查

**文件**: `public/js/app.js`

**提示词**:
```
检查 app.js 中所有 keydown 事件监听器，确保 Enter 键触发前也有空值检查。

需要检查的位置：
1. hostInput keydown → 调用 AIHostMode.handleInput → 已有 trim 检查 ✓
2. hostBlindInput keydown → submitBlindGuess → 已有检查 ✓
3. guessInput keydown → onFreeInput → 已有检查 ✓
4. turtleHostInput keydown → handleInput → 已有检查 ✓
5. turtleGuessInput keydown → onFreeInput → 已有检查 ✓
6. 各 review input keydown → handleReviewInput → 已有检查 ✓

额外需要检查的按钮点击：
- btn-turtle-host-submit-guess：已有 guessText 检查 ✓
- btn-turtle-guess-submit-surface：onSubmitSurface 中 text.trim() 检查 ✓

确认结论：当前代码已有基本空值检查，但建议在以下地方添加 input.value.trim() 的前置检查：
- guess-review-input 的 Enter → 如果为空不触发 click
- turtle-host-review-input 的 Enter → 同上
- turtle-guess-review-input 的 Enter → 同上

在各 keydown 监听中添加：
if (e.key === 'Enter' && !e.isComposing && e.target.value.trim()) { ...click() }
```

---

### 4e. API Key 验证失败友好提示
- [ ] 改进 `validateApiKey` 和错误显示

**文件**: `public/js/api.js`、`public/js/app.js`

**提示词**:
```
修改 api.js 的 validateApiKey 函数：
- 当前只是发一条消息看有没有回复
- 改为捕获具体错误并返回更友好的消息

async function validateApiKey(apiKey) {
  try {
    const messages = [{ role: 'user', content: '请回复"OK"' }];
    const text = await chatFull(messages, apiKey);
    return text.length > 0;
  } catch (err) {
    const msg = (err.message || '').toLowerCase();
    if (msg.includes('401') || msg.includes('key') || msg.includes('auth')) {
      throw new Error('API Key 无效或已过期，请检查后重试');
    }
    if (msg.includes('network') || msg.includes('fetch')) {
      throw new Error('无法连接到服务器，请检查网络');
    }
    if (msg.includes('timeout')) {
      throw new Error('验证超时，请稍后再试');
    }
    throw new Error('验证失败：' + err.message);
  }
}

同时在 app.js 的验证按钮点击处理中，将错误消息直接展示：
catch (err) {
  keyError.textContent = err.message;  // 不再拼接"验证失败: "前缀
  keyError.classList.remove('hidden');
}
```

---

## 方向 5: 代码质量

### 5a. 提取公共 JSON 流处理
- [ ] 将 `stripJsonLine` 和流式 JSON 解析逻辑提取到 `ui.js`

**文件**: `public/js/ui.js`、`public/js/ai-guess-mode.js`、`public/js/turtle-soup.js`

**提示词**:
```
在 ui.js 中新增：

function stripJsonLine(text) {
  const nlIdx = text.indexOf('\n');
  if (nlIdx === -1) return text.trimStart().startsWith('{') ? '' : text;
  return text.substring(nlIdx + 1).replace(/^\s+/, '');
}

function createJsonStreamHandler(onDisplay) {
  let jsonDone = false;
  let jsonBuffer = '';
  return {
    onChunk(chunk) {
      if (!jsonDone) {
        jsonBuffer += chunk;
        const nlIdx = jsonBuffer.indexOf('\n');
        if (nlIdx !== -1) {
          jsonDone = true;
          const afterJson = jsonBuffer.substring(nlIdx + 1).replace(/^\s+/, '');
          if (afterJson) onDisplay(afterJson);
        } else if (!jsonBuffer.trimStart().startsWith('{')) {
          jsonDone = true;
          onDisplay(jsonBuffer);
        }
      } else {
        onDisplay(chunk);
      }
    },
    reset() { jsonDone = false; jsonBuffer = ''; }
  };
}

然后从 ai-guess-mode.js 和 turtle-soup.js 中：
1. 删除各自定义的 stripJsonLine 函数
2. 在 askNext 方法中，替换内联的 jsonDone/jsonBuffer 逻辑为：
   const handler = createJsonStreamHandler((text) => appendToMsg(div, text));
   // chatStream 的 onChunk 改为 handler.onChunk
```

---

### 5b. 提取公共复盘逻辑
- [ ] 将四种模式的 review 逻辑统一为 `ReviewPhase` 对象

**文件**: `public/js/ui.js`（新增 ReviewPhase）

**提示词**:
```
在 ui.js 中新增：

const ReviewPhase = {
  async start(config) {
    // config: { chatArea, inputArea, reviewArea, loadingId, messages, apiKey, reviewPrompt }
    addMsg(config.chatArea, '━━━ 复盘阶段 ━━━', 'system');
    config.inputArea.classList.add('hidden');
    config.reviewArea.classList.remove('hidden');
    config.chatArea.scrollTop = config.chatArea.scrollHeight;

    showLoading(config.loadingId);
    try {
      const div = streamMsg(config.chatArea, 'ai');
      await chatStream(
        [...config.messages, { role: 'user', content: config.reviewPrompt }],
        config.apiKey,
        (chunk) => appendToMsg(div, chunk),
        (text) => { config.messages.push({ role: 'assistant', content: text }); }
      );
      hideLoading(config.loadingId);
    } catch (err) {
      hideLoading(config.loadingId);
      addMsg(config.chatArea, '复盘出错: ' + err.message, 'system');
    }
  },

  async handleInput(config, text) {
    const trimmed = text.trim();
    if (!trimmed) return;
    config.reviewInput.value = '';
    addMsg(config.chatArea, trimmed, 'user');

    showLoading(config.loadingId);
    try {
      const div = streamMsg(config.chatArea, 'ai');
      await chatStream(
        [...config.messages, { role: 'user', content: trimmed }],
        config.apiKey,
        (chunk) => appendToMsg(div, chunk),
        (text) => { config.messages.push({ role: 'assistant', content: text }); }
      );
      hideLoading(config.loadingId);
    } catch (err) {
      hideLoading(config.loadingId);
      addMsg(config.chatArea, '回复出错: ' + err.message, 'system');
    }
  }
};

然后简化各模式的 startReview 和 handleReviewInput：
- AIHostMode.startReview → 调用 ReviewPhase.start({ chatArea: ..., ... })
- AIHostMode.handleReviewInput → 调用 ReviewPhase.handleInput(config, text)
- 其他三个模式同理
```

---

### 5c. 统一错误处理
- [ ] 提取 `handleGameError` 统一处理游戏中的错误

**文件**: `public/js/ui.js`

**提示词**:
```
在 ui.js 中新增：

function handleGameError(loadingId, chatArea, err) {
  hideLoading(loadingId);
  const msg = (err.message || '').toLowerCase();
  let userMsg;
  if (msg.includes('network') || msg.includes('fetch')) {
    userMsg = '网络不太稳定，请重试';
  } else if (msg.includes('timeout')) {
    userMsg = 'AI 思考太久了，请重试';
  } else {
    userMsg = '出错了: ' + err.message;
  }
  addMsg(chatArea, userMsg, 'system');
}

然后在所有 catch 块中替换：
  // 旧
  hideLoading('host-loading');
  addMsg($('#host-chat-area'), '出错了: ' + err.message, 'system');
  // 新
  handleGameError('host-loading', $('#host-chat-area'), err);

需要替换的位置（约15处）：
- ai-host-mode.js: start, submitBlindGuess, handleInput, startReview, handleReviewInput
- ai-guess-mode.js: askNext, startReview, handleReviewInput
- turtle-soup.js: TurtleHostMode (start, handleInput, handleGuess, startReview, handleReviewInput)
                 TurtleGuessMode (askNext, startReview, handleReviewInput)
```

---

### 5d. app.js 事件绑定模块化
- [ ] 将 DOMContentLoaded 回调拆分为 6 个子函数

**文件**: `public/js/app.js`

**提示词**:
```
将 app.js 的 DOMContentLoaded 回调拆分为 6 个独立函数：

function initWelcome() {
  // 包含：tab 切换、内置模型卡片选择、key 显示切换、预设/自定义模型使用、
  //       确认按钮点击、恢复保存的偏好设置
  // 约 130 行
}

function initGameHall() {
  // 包含：游戏卡片点击、模式选择返回、返回欢迎页
  // 约 15 行
}

function initHostGame() {
  // 包含：host-input 发送、盲猜相关、提示/放弃/重来按钮、review 相关
  // 约 50 行
}

function initGuessGame() {
  // 包含：准备好了、回答按钮、自由输入、猜对/猜错、提示、纠错、review
  // 约 50 行
}

function initTurtleHost() {
  // 包含：turtle-host 输入/提示/放弃/猜真相/重来/ review
  // 约 40 行
}

function initTurtleGuess() {
  // 包含：提交汤面、回答按钮、自由输入、猜对/猜错、review
  // 约 30 行
}

document.addEventListener('DOMContentLoaded', () => {
  initWelcome();
  initGameHall();
  initHostGame();
  initGuessGame();
  initTurtleHost();
  initTurtleGuess();
  // 结果页面按钮绑定（约10行，可以内联）
});

注意事项：
- 各函数内部逻辑完全不变，只是拆分位置
- 变量作用域在各自的函数内，不会有冲突
- DOMContentLoaded 只负责调用 6 个 init 函数和绑定结果页按钮
```

---

## 方向 3: 游戏机制调整

### 3a. 实时分数显示
- [ ] 在 AI 出题模式侧边栏添加可见的分数条
- [ ] 分数变化时有动画递减效果

**文件**: `public/index.html`、`public/css/style.css`、`public/js/ui.js`

**提示词**:
```
HTML — 在 host-sidebar 的 host-stats-row 中添加：
  <span class="stat-chip">💰分数 <strong id="host-score">100</strong></span>

CSS — 添加分数变化动画：
  .stat-chip strong.score-drop {
    animation: scoreDrop 0.4s ease;
    color: var(--red);
  }
  @keyframes scoreDrop {
    0% { transform: scale(1); }
    50% { transform: scale(1.3); color: #e53935; }
    100% { transform: scale(1); }
  }

JS — 修改 ui.js 的 updateHostScore 函数（当前是空操作）：
  function updateHostScore(oldScore) {
    const el = $('#host-score');
    if (!el) return;
    const newScore = GameState.host.score;
    el.textContent = newScore;
    if (oldScore !== undefined && newScore < oldScore) {
      el.classList.add('score-drop');
      setTimeout(() => el.classList.remove('score-drop'), 400);
    }
  }

同时确保 ai-host-mode.js 中所有调用 updateHostScore 的地方都传入了 oldScore 参数（当前已传入但被忽略）。
```

---

### 3b. 难度影响 AI 行为
- [ ] 在 AI 出题模式的 system prompt 中注入难度参数

**文件**: `public/js/prompts.js` — `PROMPTS.aiHostSystem`

**提示词**:
```
修改 aiHostSystem 为接受 difficulty 参数的函数：

PROMPTS.aiHostSystem = (difficulty) => {
  const difficultyRules = {
    easy: `
## 简单模式特殊规则
- 回答问题时可以附带一句简短的解释（如"是——此人确实是皇帝，建立了统一的王朝"）
- 给线索时偏向使用更具体的描述（如"L1: 此人是中国古代的统治者"）
- 当玩家连续2次猜错时，主动给出一条额外线索`,
    medium: `
## 中等模式特殊规则
- 回答问题时只回答四个选项之一，不加解释
- 给线索时使用中等粒度的描述`,
    hard: `
## 困难模式特殊规则
- 回答问题时尽量简洁，不加任何额外信息
- 给线索时使用最模糊的描述（如"L1: 此人活跃于人类有文字记载的历史时期"）
- 如果玩家的问题过于直接（如直接问姓名特征），可以回答"正史无记载"来增加难度
- 不要主动给出额外线索`
  };
  return `你是一个历史人物猜谜游戏的主持人。...（原内容）
${difficultyRules[difficulty] || ''}`;
};

然后修改 ai-host-mode.js 中初始化 GameState.messages 的地方，将：
  content: PROMPTS.aiHostSystem + '\n\n你选择的人物是：...'
改为：
  content: PROMPTS.aiHostSystem(difficulty) + '\n\n你选择的人物是：...'
```

---

### 3c. "再来一局"优化
- [ ] 将结果页按钮改为选项弹窗

**文件**: `public/index.html`、`public/css/style.css`、`public/js/app.js`

**提示词**:
```
HTML — 在结果页的 result-actions 区域替换为：
  <div class="result-actions">
    <button id="btn-play-again" class="btn-primary">再来一局</button>
    <button id="btn-switch-mode" class="btn-secondary">换模式</button>
  </div>
  <!-- 再来一局选项弹窗 -->
  <div id="play-again-modal" class="modal hidden">
    <div class="modal-overlay"></div>
    <div class="modal-content">
      <h3>选择下一步</h3>
      <button id="btn-replay-same" class="btn-primary">再来一局（同难度）</button>
      <button id="btn-replay-diff" class="btn-secondary">换个难度</button>
      <button id="btn-replay-hall" class="btn-secondary">返回游戏厅</button>
    </div>
  </div>

CSS — 添加 modal 样式：
  .modal { position: fixed; inset: 0; z-index: 100; display: flex; align-items: center; justify-content: center; }
  .modal-overlay { position: absolute; inset: 0; background: rgba(0,0,0,0.4); }
  .modal-content { position: relative; background: var(--cream); border: 2px solid var(--border);
    border-radius: 12px; padding: 24px; min-width: 280px; text-align: center; z-index: 1; }
  .modal-content h3 { margin-bottom: 16px; color: var(--brown); }
  .modal-content button { display: block; width: 100%; margin-bottom: 10px; }

JS — 修改 app.js 中 btn-play-again 的点击事件：
  $('#btn-play-again').addEventListener('click', () => {
    $('#play-again-modal').classList.remove('hidden');
  });
  $('#btn-replay-same').addEventListener('click', () => {
    $('#play-again-modal').classList.add('hidden');
    const prevMode = GameState.mode;
    GameState.reset();
    if (prevMode === 'ai-guess') AIGuessMode.start();
    else if (prevMode === 'turtle-guess') TurtleGuessMode.start();
    else resetToGameHall(); // 同难度需要重新进入流程，或记录上次难度
  });
  $('#btn-replay-diff').addEventListener('click', () => {
    $('#play-again-modal').classList.add('hidden');
    resetToGameHall();
  });
  $('#btn-replay-hall').addEventListener('click', () => {
    $('#play-again-modal').classList.add('hidden');
    resetToGameHall();
  });
  // 点击 overlay 关闭弹窗
  document.querySelector('#play-again-modal .modal-overlay')?.addEventListener('click', () => {
    $('#play-again-modal').classList.add('hidden');
  });
```

---

## 附录：执行建议

| 方向 | 改动文件数 | 风险等级 | 建议顺序 |
|------|-----------|---------|---------|
| 1. UI/UX | 2-3 | 低 | 纯 CSS/展示层，不影响游戏逻辑 |
| 2. Prompt | 2 | 中 | 只改 prompt 文本，可能需要几轮调试 |
| 4. 边缘情况 | 4 | 中 | 添加容错逻辑，需测试各种异常场景 |
| 5. 代码质量 | 5 | 中 | 重构需仔细测试各模式功能完整性 |
| 3. 游戏机制 | 3 | 高 | 改变游戏体验，建议最后做 |

**推荐工作流**：每完成一个方向后，`npm start` 启动游戏，完整测试 4 种游戏模式的基本流程。
